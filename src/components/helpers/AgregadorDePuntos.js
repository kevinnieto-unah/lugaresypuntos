import React from 'react'
import { addPuntosDelLugar } from "../../actions/puntos";
import { useDispatch } from "react-redux";

export const AgregadorDePuntos = ({row}) => {
    console.log(row);

    
}
